import { atom } from '../atom/atom'

/**
 * @deprecated since version 1.0.0beta-12
 *
 * `create` as a named export is deprecated. In the future versions, use one of the following instead:
 * @example
 * // Recommended
 * import { atom } from 'xoid'
 */
export const create = atom
